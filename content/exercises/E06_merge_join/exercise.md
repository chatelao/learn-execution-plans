# Merge Join Exercise

In this exercise, you need to join the `orders` and `order_items` tables in a way that triggers a Merge Join.

Hint: Merge joins often require the input data to be sorted by the join keys.
